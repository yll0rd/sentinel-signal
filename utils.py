def mins_to_secs(minutes: int) -> int:
    """Convert minutes to seconds."""
    return minutes * 60

def mins_to_secs_in_str(minutes: int) -> str:
    """Convert minutes to seconds in string format."""
    return str(mins_to_secs(minutes))